#include <vector>

bool IsAbundant(int x); // Checks if x is abundant
std::vector<int> GetProperDivisors(int x); // Gets the proper divisors of x
int Sum(const std::vector<int> &numbers); // Returns the sum of a the vector
bool CheckSum(int x,const std::vector<int> &numbers); // Returns true if x can be 
// written as the sum of 2 numbers in the vector